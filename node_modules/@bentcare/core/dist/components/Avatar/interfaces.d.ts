import { SIZES_TYPES } from './constants';
export interface InitialStyleProps {
    size: SIZES_TYPES;
}
export interface AvatarStyleProps {
    /**
     Use the loading state to indicate that the data Avatar needs is still loading.
    */
    loading?: boolean;
    /**
      Avatar falls back to the user’s initial when no image is provided. Supply a `username` and omit `src` to see what this looks like.
    */
    username?: string;
    /**
      The URL of the Avatar's image.
    */
    src?: string;
    /**
      Avatar comes in four sizes. In most cases, you’ll be fine with `medium`.
    */
    size?: SIZES_TYPES;
}
export interface AvatarProps extends AvatarStyleProps {
    style?: any;
    className?: string;
}
//# sourceMappingURL=interfaces.d.ts.map